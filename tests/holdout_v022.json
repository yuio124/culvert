{
  "_comment": "v0.2.2 holdout. Labels were fixed BEFORE the classifier rules were written (2026-08-25) and do not duplicate any development test command. expect: allow | deny:<rule>",
  "bounded_variants": [
    {
      "id": "H1",
      "cmd": "sqlite3 events.db \"SELECT COUNT(*) FROM logs WHERE level='ERROR'\"",
      "expect": "allow"
    },
    {
      "id": "H2",
      "cmd": "sqlite3 events.db \"SELECT avg(length(message)) FROM logs\"",
      "expect": "allow"
    },
    {
      "id": "H3",
      "cmd": "sqlite3 events.db \"SELECT * FROM logs LIMIT 5\"",
      "expect": "deny:db-query",
      "note": "not statically scalar - conservative deny"
    },
    {
      "id": "H4",
      "cmd": "sqlite3 events.db \"SELECT service, COUNT(*) FROM logs GROUP BY service\"",
      "expect": "deny:db-query",
      "note": "GROUP BY row count is statically unknown"
    },
    {
      "id": "H5",
      "cmd": "find . -name '*.py' | wc -l",
      "expect": "allow"
    },
    {
      "id": "H6",
      "cmd": "grep -rn \"token\" . | head -30",
      "expect": "allow"
    },
    {
      "id": "H7",
      "cmd": "grep -rn \"token\" . | head -500",
      "expect": "deny:recursive-search",
      "note": "cap exceeds the 100-line bound"
    },
    {
      "id": "H8",
      "cmd": "rg handler | tail -20",
      "expect": "allow"
    },
    {
      "id": "H9",
      "cmd": "cat __BIGFILE__ | head -50",
      "expect": "allow"
    },
    {
      "id": "H10",
      "cmd": "wc -l app.log",
      "expect": "allow"
    }
  ],
  "node_test_variants": [
    {
      "id": "H11",
      "cmd": "npm test",
      "expect": "deny:test-run"
    },
    {
      "id": "H12",
      "cmd": "npm run test 2>&1 | tail -25",
      "expect": "deny:test-run",
      "note": "test_run is never bounded-exempt (conservative)"
    },
    {
      "id": "H13",
      "cmd": "node --test test/unit/",
      "expect": "deny:test-run"
    },
    {
      "id": "H14",
      "cmd": "npx vitest run",
      "expect": "deny:test-run"
    },
    {
      "id": "H15",
      "cmd": "pnpm test",
      "expect": "deny:test-run"
    },
    {
      "id": "H16",
      "cmd": "yarn test --silent",
      "expect": "deny:test-run"
    },
    {
      "id": "H17",
      "cmd": "npm run build",
      "expect": "allow",
      "note": "build is out of scope - documented"
    },
    {
      "id": "H18",
      "cmd": "npm install",
      "expect": "allow"
    }
  ],
  "fan_out_variants": [
    {
      "id": "H19",
      "cmd": "for f in src/*.js; do echo $f; cat \"$f\"; done",
      "expect": "deny:fan-out-read"
    },
    {
      "id": "H20",
      "cmd": "cat src/*.js",
      "expect": "deny:fan-out-read"
    },
    {
      "id": "H21",
      "cmd": "find . -name '*.md' -exec cat {} \\;",
      "expect": "deny:fan-out-read"
    },
    {
      "id": "H22",
      "cmd": "find src -type f | xargs cat",
      "expect": "deny:fan-out-read"
    },
    {
      "id": "H23",
      "cmd": "cat src/*.js | wc -l",
      "expect": "allow",
      "note": "final stage is scalar wc"
    },
    {
      "id": "H24",
      "cmd": "cat README.md",
      "expect": "allow",
      "note": "single small file"
    }
  ],
  "light_commands": [
    {
      "id": "H25",
      "cmd": "ls -la src",
      "expect": "allow"
    },
    {
      "id": "H26",
      "cmd": "git status --short",
      "expect": "allow"
    },
    {
      "id": "H27",
      "cmd": "python3 scripts/check.py",
      "expect": "allow"
    },
    {
      "id": "H28",
      "cmd": "grep -n \"def main\" app.py",
      "expect": "allow"
    },
    {
      "id": "H29",
      "cmd": "echo hello && date",
      "expect": "allow"
    },
    {
      "id": "H30",
      "cmd": "sed -n '1,40p' server.py",
      "expect": "allow"
    }
  ],
  "long_command_v023": [
    {
      "id": "L1",
      "cmd": "for f in a/plugin.json b/plugin.json c/plugin.json; do jq -r .version \"$f\" | head -8; done && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad && echo pad",
      "expect": "allow",
      "note": "~700 chars, every risky segment head-capped - bounded beats length"
    },
    {
      "id": "L2",
      "cmd": "python3 tools/report.py --input data.bin --mode full --verbose --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding --flag-opt-value-padding ",
      "expect": "deny:long-command",
      "note": ">1200 chars and output statically unknown"
    },
    {
      "id": "L3",
      "cmd": "grep -rn pattern .",
      "expect": "deny:recursive-search",
      "note": "short but recursive output risk - unchanged"
    },
    {
      "id": "L4",
      "cmd": "cat > /tmp/x.py <<EOF\nprint(1)\nEOF",
      "expect": "deny:heredoc",
      "note": "heredoc unchanged in v0.2.3"
    },
    {
      "id": "L5",
      "cmd": "grep -c token0 app.log && grep -c token1 app.log && grep -c token2 app.log && grep -c token3 app.log && grep -c token4 app.log && grep -c token5 app.log && grep -c token6 app.log && grep -c token7 app.log && grep -c token8 app.log && grep -c token9 app.log && grep -c token10 app.log && grep -c token11 app.log && grep -c token12 app.log && grep -c token13 app.log && grep -c token14 app.log && grep -c token15 app.log && grep -c token16 app.log && grep -c token17 app.log && grep -c token18 app.log && grep -c token19 app.log && grep -c token20 app.log && grep -c token21 app.log && grep -c token22 app.log && grep -c token23 app.log && grep -c token24 app.log && grep -c token25 app.log && grep -c token26 app.log && grep -c token27 app.log && grep -c token28 app.log && grep -c token29 app.log && grep -c token30 app.log && grep -c token31 app.log && grep -c token32 app.log && grep -c token33 app.log && grep -c token34 app.log && grep -c token35 app.log && grep -c token36 app.log && grep -c token37 app.log && grep -c token38 app.log && grep -c token39 app.log && grep -c token40 app.log && grep -c token41 app.log && grep -c token42 app.log && grep -c token43 app.log && grep -c token44 app.log && grep -c token45 app.log && grep -c token46 app.log && grep -c token47 app.log",
      "expect": "allow",
      "note": ">1200 chars but every segment is a bounded grep -c"
    },
    {
      "id": "L6",
      "cmd": "python3 tools/report.py --input data.bin --some-longer-option-name --some-longer-option-name --some-longer-option-name --some-longer-option-name --some-longer-option-name --some-longer-option-name --some-longer-option-name --some-longer-option-name --some-longer-option-name --some-longer-option-name --some-longer-option-name --some-longer-option-name --some-longer-option-name --some-longer-option-name --some-longer-option-name --some-longer-option-name --some-longer-option-name --some-longer-option-name --some-longer-option-name --some-longer-option-name --some-longer-option-name --some-longer-option-name --some-longer-option-name --some-longer-option-name --some-longer-option-name --some-longer-option-name ",
      "expect": "allow",
      "note": "~700 chars opaque output - below the raised 1200 threshold"
    }
  ]
}
